sources:
  podman_events:
    type: exec
    command: [podman, --url, unix://run/podman/podman.sock, events, --format, json, --streaming]
    mode: streaming
    decoding:
      codec: json

sinks:
  console:
    type: console
    inputs:
      - podman_events
    encoding:
      codec: json